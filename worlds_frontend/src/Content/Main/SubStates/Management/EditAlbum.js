import {useParams, useNavigate} from 'react-router-dom';
import {Grid} from "@mui/material";

export default function EditAlbum({ worldsManagementContract }) {
    let {worldId, albumId} = useParams();
    return <></>;
}
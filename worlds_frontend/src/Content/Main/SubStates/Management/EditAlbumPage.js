import {useParams, useNavigate} from 'react-router-dom';
import {Grid} from "@mui/material";

export default function EditAlbumPage({ worldsManagementContract }) {
    let {worldId, albumId, pageId} = useParams();
    return <></>;
}
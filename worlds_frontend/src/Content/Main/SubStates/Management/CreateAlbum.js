import {useParams, useNavigate} from 'react-router-dom';
import {Grid} from "@mui/material";

export default function CreateAlbum({ worldsManagementContract }) {
    let {worldId} = useParams();
    return <></>;
}